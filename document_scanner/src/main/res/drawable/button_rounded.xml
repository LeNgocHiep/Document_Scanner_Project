<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/rounded_button.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@android:color/darker_gray" /> <!-- Button background color -->

    <!-- Corner radius -->
    <corners android:radius="25dp" /> <!-- Adjust the radius for rounded corners -->

    <!-- Border (stroke) properties -->
<!--    <stroke-->
<!--        android:width="2dp"-->
<!--        android:color="@android:color/black" /> &lt;!&ndash; Border color &ndash;&gt;-->

    <!-- Padding (optional, for inner space) -->
    <padding
        android:left="5dp"
        android:top="5dp"
        android:right="5dp"
        android:bottom="5dp" />
</shape>
